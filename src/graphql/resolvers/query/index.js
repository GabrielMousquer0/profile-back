const { users } = require('./users');
const { viewUser } = require('./viewUser');
const { languagesUser } = require('./languagesUser');
const { searchUser } = require('./searchUser');

module.exports = { users, languagesUser, viewUser, searchUser };
